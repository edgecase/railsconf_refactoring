<ul class="dice">
  <% roll_data.roll.each do |face| -%>
    <li><%= image_tag "die#{face}.gif" %></li>
  <% end -%>
  <li>(<%= roll_data.roll_points %> points in roll)</li>
</ul>
<p>
  Score for this turn: <%= roll_data.turn_score %>
</p>
<h3>
  <%= player.name %>
  <%=
     case roll_data.action
     when :hold
       "held"
     when :bust
       "went bust"
     when :roll
       "rolled again"
     else
       "-Unknown action #{roll_data.action}-"
     end
 -%>
</h3>
